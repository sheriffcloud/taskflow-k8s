apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: taskflow
  labels:
    app: task-service
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
