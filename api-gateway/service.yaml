apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: taskflow
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
